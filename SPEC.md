# タスク管理アプリ 仕様書

作成日: 2026-08-29

## 1. 背景と課題

- 日付だけでタスクを並べると、複数プロジェクトが1日の中で混在し、
  「今日どのプロジェクトをどこまで進めたか」が見えなくなる。
- プロジェクトを大枠（入れ物）とし、その中のタスクを日付で回したい。
- ただし「プロジェクトごとのカレンダー」ではなく、
  **プロジェクト軸と日付軸を同時に見せる（共存）** ことが狙い。

## 2. コアコンセプト

### メインビュー：日付カラム ＋ プロジェクト色分け（グループ化）

- 横方向に日付カラムを並べる（週単位でスクロール、今日を強調）。
- 各日付カラムの中は、**プロジェクトごとにグループ化**して表示する。
  - 単に色付きタスクを散らすのではなく、プロジェクト小見出し＋色チップで塊にする。
  - これにより「日付軸で並ぶが、プロジェクトは混ざらない」を実現。
- 各プロジェクトには識別色を割り当てる（washi / sumi / shu の方向性を踏襲）。

```
              8/29(金)          8/30(土)          8/31(日)
          ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
期間バー帯 │◆ ProjA 実装 ══╪═════════════════╪══════▶        │  ← 複数日にまたがるバー
          │        ◆ ProjB 校正 ═══════▶     │              │
          ├──────────────┤ ├──────────────┤ ├──────────────┤
単発タスク │ ■ ProjA(朱)   │ │ ■ ProjA(朱)   │ │ ■ ProjC(墨)   │
          │   ・要件整理  │ │   ・API設計   │ │   ・撮影      │
          │   ・見積送付✓ │ │              │ │              │
          │ ■ ProjB(藍)   │ │ ■ ProjC(墨)   │ │              │
          │   ・原稿確認  │ │   ・撮影準備  │ │              │
          └──────────────┘ └──────────────┘ └──────────────┘
```

- **上段：期間バー帯** … 開始日〜終了日を持つタスクを、日付カラムをまたぐ横バーで表示（プロジェクト色）。
- **下段：単発タスク** … 特定の1日に属するタスク。プロジェクトごとにグループ化。

## 3. ビュー切り替え（同一データを3視点で）

1. **カレンダービュー（メイン）** … 上図。プロジェクト横断で今週の流れを俯瞰。
2. **プロジェクトビュー** … 1プロジェクトに絞り、その中のタスクを日付順に縦に並べる。集中作業向き。
3. **デイビュー** … 1日だけを見て、プロジェクト別にグルーピング。「今日やること」を混ぜずに把握。

データモデルは1つ。ビューは見せ方の違いのみ。

## 4. 主要インタラクション

- **ドラッグ移動**
  - 別の日付カラムへ移動 → リスケジュール（予定日変更）。
  - 期間バーの端をドラッグ → 開始/終了日の伸縮。
- **プロジェクト別フィルタ** … 特定プロジェクトの表示ON/OFF（色チップのトグル）。
- **完了操作** … チェックで完了。完了タスクは淡色化（色＝達成の方向性）。
- **クイック追加** … 日付カラム内の「＋」から、そのプロジェクト・その日にタスク追加。

## 5. データモデル（最小）

### Project
| フィールド | 型 | 説明 |
|---|---|---|
| id | string | 識別子 |
| name | string | プロジェクト名 |
| color | string | 識別色 |
| status | enum | 進行中 / 完了 / 保留 |
| order | number | 表示順 |

### Task
| フィールド | 型 | 説明 |
|---|---|---|
| id | string | 識別子 |
| project_id | string | 所属プロジェクト |
| title | string | タスク名 |
| kind | enum | `single`（単発） / `range`（期間） |
| date | date | kind=single のときの予定日 |
| start_date | date | kind=range の開始日 |
| end_date | date | kind=range の終了日 |
| done | boolean | 完了フラグ |
| order | number | 同一日・同一PJ内の並び順 |

- 単発タスクは `date` のみ、期間タスクは `start_date`/`end_date` を使う。
- どちらの kind でも `project_id` は必須 → プロジェクト無所属タスクは作らない。

## 6. デザイン方向性

- laptop 想定の二ペインレイアウト（左：プロジェクト一覧 / 右：カレンダービュー）を基本とする。
- 配色は washi（地）/ sumi（文字・枠）/ shu（強調）をベースに、
  プロジェクト色を加える。**色＝達成の可視化**（完了で淡く/満ちる表現）。

## 7. 段階的な実装スコープ

- **Phase 1（MVP）** … Project / 単発 Task / カレンダービュー / ドラッグでリスケ / 完了。
- **Phase 2** … 期間バー（range Task）、プロジェクトビュー、デイビュー。
- **Phase 3** … フィルタ、達成の色表現、並び替え、検索。
